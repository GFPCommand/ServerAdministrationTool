@model ErrorViewModel
@{
    ViewData["Title"] = "Authorization";
}

<style>
    header #side-checkbox, header .side-panel, header .side-button-1-wr {
    display: none;
    }

    header .navbar-brand {
    pointer-events: none;
    cursor: default;
    }
</style>

<div class="auth-block">
    <h2>Авторизация</h2>
    <form method="post" asp-controller="Auth" asp-action="Authorization">
        <div class="form-group">
            <label>Введите логин:</label>
            <input type="text" name="login" required><br>
        </div>
        <div class="form-group">
            <label>Введите пароль:</label>
            <input type="password" name="pswd" required><br>
        </div>
        @if (!Model.IsSuccess)
        {
            <p style="color: red">Логин и/или пароль неверны</p>
        }
        <div class="form-group">
            <input type="submit" value="Войти" class="btn" style="background-color: rgb(130, 169, 240);">
        </div>
    </form>
</div>